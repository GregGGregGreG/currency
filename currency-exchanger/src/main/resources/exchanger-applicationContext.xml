<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <context:component-scan base-package="org.baddev.currency.exchange"/>
    <context:property-placeholder location="
        classpath:schedule.properties,
        classpath:api_sources.properties,
        classpath:proxy.properties,
        classpath:db.properties"/>

    <task:annotation-driven scheduler="myScheduler"/>
    <task:scheduler id="myScheduler"/>

    <!-- import -->
    <bean id="fetcherContext" class="org.baddev.currency.fetcher.config.FetcherConfig"/>
    <import resource="dao-applicationContext.xml"/>

</beans>